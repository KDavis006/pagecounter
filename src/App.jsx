// import the counter
import Counter from './Components/PageViewCounter'

// runs the app
const App = () => {
  return (
    // has the counter appear on the page
    <Counter/>
  )
}

// exports the app so that the index file can run it
export default App